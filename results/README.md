# Results directory

This folder is populated after running the experiments.

After running `python run_all_experiments.py`, you will find:

- `experiment1_results.json` — prompt injection detection metrics
- `experiment2_results.json` — RBAC enforcement metrics  
- `experiment3_results.json` — anomaly detection metrics
- `consolidated_results.json` — combined Section V summary
